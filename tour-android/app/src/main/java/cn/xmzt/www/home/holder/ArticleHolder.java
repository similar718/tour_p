/*
package com.jm.selfdriving.home.holder;

import android.content.Context;
import android.view.LayoutInflater;
import android.view.View;
import android.widget.ImageView;

import com.jm.selfdriving.R;
import com.jm.selfdriving.glide.GlideUtil;
import com.jm.selfdriving.home.beanUtils.AdvertiseBean;
import com.ms.banner.holder.BannerViewHolder;
*/
/**
 * Created by Averysk
 *//*

public class ArticleHolder implements BannerViewHolder<AdvertiseBean> {

    private ImageView imageView;

    @Override
    public View createView(Context context) {
        View view = LayoutInflater.from(context).inflate(R.layout.viewpager_page_img, null);
        imageView = view.findViewById(R.id.imageView);
        return view;
    }

    @Override
    public void onBind(Context context, int position, AdvertiseBean data) {
        // 数据绑定
        GlideUtil.loadImgC(imageView,data.getAdvPic());
    }
}
*/
